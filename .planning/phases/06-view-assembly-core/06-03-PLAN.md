---
phase: 06-view-assembly-core
plan: 03
type: execute
wave: 1
depends_on: ["06-02"]
files_modified:
  - scripts/view_assembler.py
  - tests/test_view_assembler.py
autonomous: true
gap_closure: true
requirements: [VIEW-06, VIEW-08]

must_haves:
  truths:
    - "View specifications are validated against the view-spec.json schema before use in the production code path"
    - "A malformed view spec passed to load_view_spec() raises a validation error before assembly proceeds"
  artifacts:
    - path: "scripts/view_assembler.py"
      provides: "SchemaValidator call in load_view_spec()"
      contains: "validate_or_raise.*view-spec"
    - path: "tests/test_view_assembler.py"
      provides: "Test confirming schema validation in load_view_spec production path"
      min_lines: 840
  key_links:
    - from: "scripts/view_assembler.py"
      to: "schemas/view-spec.json"
      via: "SchemaValidator.validate_or_raise('view-spec', spec) inside load_view_spec()"
      pattern: "validate_or_raise.*view-spec"
---

<objective>
Close the one verification gap from Phase 6: add SchemaValidator validation of view-spec JSON in the load_view_spec() production code path.

Purpose: The view-spec.json schema exists and is correct, but load_view_spec() does not enforce it. A malformed spec will silently produce incorrect results or fail at a downstream point. This gap was identified in 06-VERIFICATION.md (truth #1: partial).

Output: Updated load_view_spec() with schema validation; updated tests confirming production path enforcement.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-view-assembly-core/06-VERIFICATION.md

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From scripts/view_assembler.py (current signature — will be modified):
```python
def load_view_spec(
    spec_path: str, parameters: dict | None = None
) -> dict:
```

From scripts/schema_validator.py:
```python
class SchemaValidator:
    def __init__(self, schemas_dir: str): ...
    def validate_or_raise(self, schema_name: str, data: dict) -> None: ...
```

The SchemaValidator is already imported in view_assembler.py (line 22).

From assemble_view() — the main caller of load_view_spec():
```python
def assemble_view(
    api: SlotAPI,
    spec: dict,
    workspace_root: str,
    schemas_dir: str,
) -> dict:
```
Note: assemble_view() already receives schemas_dir and creates a SchemaValidator for output validation. The spec dict is passed in already loaded — so validation should happen inside load_view_spec() itself, which requires adding a schemas_dir parameter.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add view-spec schema validation to load_view_spec()</name>
  <files>scripts/view_assembler.py, tests/test_view_assembler.py</files>
  <behavior>
    - Test 1: load_view_spec() with a valid spec file and schemas_dir validates successfully and returns the spec dict (existing behavior preserved)
    - Test 2: load_view_spec() with an invalid spec (e.g., missing required "name" field) and schemas_dir raises a validation error before returning
    - Test 3: load_view_spec() without schemas_dir (None/omitted) skips validation and returns the spec dict (backward compatibility)
  </behavior>
  <action>
    1. Add `schemas_dir: str | None = None` parameter to `load_view_spec()` signature (after `parameters`, defaulting to None for backward compatibility).

    2. After loading JSON and resolving parameters (line 96, before `return spec`), add schema validation:
       ```python
       if schemas_dir is not None:
           validator = SchemaValidator(schemas_dir)
           validator.validate_or_raise("view-spec", spec)
       ```

    3. Update the docstring to document the new parameter and the ValidationError raise condition.

    4. Add tests:
       - Test that load_view_spec with schemas_dir validates against view-spec.json schema (valid spec passes)
       - Test that load_view_spec with schemas_dir and invalid spec raises jsonschema.exceptions.ValidationError
       - Test that load_view_spec without schemas_dir (backward compat) does NOT call SchemaValidator

    5. Ensure all existing tests still pass — the new parameter defaults to None so no existing call sites break.

    Note: The validation must happen AFTER parameter resolution so that resolved patterns are validated, not raw {variable} patterns. The view-spec.json schema validates the structure (required fields: name, description, scope_patterns) not the pattern content, so this ordering is correct.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/skills/system-dev && python -m pytest tests/test_view_assembler.py tests/test_view_integration.py -x -q</automated>
  </verify>
  <done>load_view_spec() calls SchemaValidator.validate_or_raise("view-spec", spec) when schemas_dir is provided. Invalid specs raise ValidationError. All existing tests pass unchanged. New tests confirm both validation and backward compatibility.</done>
</task>

</tasks>

<verification>
1. `grep -n "validate_or_raise.*view-spec" scripts/view_assembler.py` returns a match inside load_view_spec()
2. `python -m pytest tests/test_view_assembler.py tests/test_view_integration.py -x -q` — all tests pass
3. `python -m pytest tests/ -x -q` — full regression passes (368+ tests)
</verification>

<success_criteria>
- load_view_spec() has schemas_dir parameter and calls SchemaValidator.validate_or_raise("view-spec", spec)
- Invalid view specs raise validation errors in the production code path
- Backward compatibility maintained (schemas_dir defaults to None, existing callers unaffected)
- All 368+ existing tests pass, plus new validation tests
</success_criteria>

<output>
After completion, create `.planning/phases/06-view-assembly-core/06-03-SUMMARY.md`
</output>
