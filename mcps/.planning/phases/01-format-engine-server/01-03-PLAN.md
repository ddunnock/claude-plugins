---
phase: 01-format-engine-server
plan: 03
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - schema-validator/src/security/path-validator.ts
  - schema-validator/src/security/atomic-write.ts
  - schema-validator/src/security/schema-loader.ts
  - schema-validator/tests/security/path-validator.test.ts
  - schema-validator/tests/security/atomic-write.test.ts
  - schema-validator/tests/security/schema-loading.test.ts
autonomous: true
requirements: [SEC-01, SEC-02, SEC-03]

must_haves:
  truths:
    - "File paths with traversal patterns (../) are rejected with a clear error including the rejected path"
    - "File paths with null bytes are rejected"
    - "File writes land atomically (no partial files on crash)"
    - "Schema files are validated for structure before being processed"
    - "Only paths within allowed directories are accepted"
  artifacts:
    - path: "schema-validator/src/security/path-validator.ts"
      provides: "Path traversal prevention"
      exports: ["validatePath", "PathValidationError"]
    - path: "schema-validator/src/security/atomic-write.ts"
      provides: "Atomic file write wrapper"
      exports: ["atomicWrite"]
    - path: "schema-validator/src/security/schema-loader.ts"
      provides: "Schema file structure validation before loading"
      exports: ["validateSchemaFile"]
  key_links:
    - from: "schema-validator/src/security/path-validator.ts"
      to: "node:path"
      via: "path.resolve for normalization, startsWith for containment check"
      pattern: "path\\.resolve"
    - from: "schema-validator/src/security/atomic-write.ts"
      to: "write-file-atomic"
      via: "writeFileAtomic for temp-file-then-rename"
      pattern: "writeFileAtomic"
---

<objective>
Implement the security layer: path traversal prevention, atomic file writes, and schema file structure validation. These are standalone security primitives that tools will use for all file operations.

Purpose: Ensure all file I/O is safe (no path traversal), reliable (no partial writes), and validated (schema files checked before dynamic use).
Output: Three security modules with comprehensive test coverage.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-format-engine-server/01-CONTEXT.md
@.planning/phases/01-format-engine-server/01-RESEARCH.md
@.planning/phases/01-format-engine-server/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01: Error types in src/formats/types.ts -->
```typescript
export class FormatError extends Error {
  code: string;
  filePath: string;
  line?: number;
  column?: number;
  suggestedFix?: string;
}
```

<!-- From Research: Path validation pattern -->
```typescript
import path from "node:path";

function validatePath(filePath: string, allowedDirs: string[]): string {
  if (filePath.includes("\0")) throw new PathValidationError(...);
  const resolved = path.resolve(filePath);
  const isAllowed = allowedDirs.some(dir => {
    const normalizedDir = path.resolve(dir) + path.sep;
    return resolved.startsWith(normalizedDir) || resolved === path.resolve(dir);
  });
  if (!isAllowed) throw new PathValidationError(...);
  return resolved;
}
```

<!-- From Research: Atomic write pattern -->
```typescript
import writeFileAtomic from "write-file-atomic";
await writeFileAtomic(filePath, content, { encoding: "utf8" });
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Path validator and atomic write modules with tests</name>
  <files>schema-validator/src/security/path-validator.ts, schema-validator/src/security/atomic-write.ts, schema-validator/tests/security/path-validator.test.ts, schema-validator/tests/security/atomic-write.test.ts</files>
  <behavior>
    - validatePath("/safe/dir/file.json", ["/safe/dir"]) returns resolved absolute path
    - validatePath("../../../etc/passwd", ["/safe/dir"]) throws PathValidationError with code "ERR_PATH_TRAVERSAL" and includes the rejected path in the message
    - validatePath("file\0.json", [...]) throws PathValidationError with code "ERR_NULL_BYTE"
    - validatePath("/outside/file.json", ["/safe/dir"]) throws PathValidationError with code "ERR_PATH_TRAVERSAL"
    - validatePath with empty allowedDirs throws
    - atomicWrite("/tmp/test.json", "content") writes file that can be read back with matching content
    - atomicWrite uses write-file-atomic (temp file + rename, not direct write)
  </behavior>
  <action>
1. Create src/security/path-validator.ts:
   - Export class PathValidationError extending Error with: code (string), rejectedPath (string)
   - Export function validatePath(filePath: string, allowedDirs?: string[]): string
     - If allowedDirs not provided, default to [process.cwd()]
     - Reject null bytes (code "ERR_NULL_BYTE")
     - Resolve path to absolute with path.resolve()
     - Check resolved path starts with at least one allowed directory (using path.resolve(dir) + path.sep prefix check, also handle exact dir match)
     - On failure: throw PathValidationError with code "ERR_PATH_TRAVERSAL", include the rejected path in message: `Path '${filePath}' is outside allowed directories`
     - On success: return resolved absolute path

2. Create src/security/atomic-write.ts:
   - Export async function atomicWrite(filePath: string, content: string, options?: { encoding?: string }): Promise<void>
   - Use write-file-atomic: await writeFileAtomic(filePath, content, { encoding: options?.encoding ?? "utf8" })
   - Wrap errors in a descriptive message including the file path

3. Create tests/security/path-validator.test.ts:
   - Valid path within allowed dir returns resolved path
   - Relative path within allowed dir resolves correctly
   - Path with ../ traversal throws ERR_PATH_TRAVERSAL
   - Path with null byte throws ERR_NULL_BYTE
   - Path outside allowed dirs throws ERR_PATH_TRAVERSAL
   - Error message includes the rejected path (user decision: path validation errors include rejected path)
   - Multiple allowed dirs: path valid in second dir succeeds
   - Default allowed dir is process.cwd()

4. Create tests/security/atomic-write.test.ts:
   - Write a file and read it back -- content matches
   - Write to nested directory that exists succeeds
   - Use a tmp directory for test isolation
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/mcps/schema-validator && bun test tests/security/path-validator.test.ts tests/security/atomic-write.test.ts</automated>
  </verify>
  <done>Path validator rejects traversal attacks with clear errors including rejected path; atomic write completes reliably; all security tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Schema file structure validator with tests</name>
  <files>schema-validator/src/security/schema-loader.ts, schema-validator/tests/security/schema-loading.test.ts</files>
  <behavior>
    - validateSchemaFile with valid JS/TS file structure (exports a schema) returns validation pass
    - validateSchemaFile with non-existent file throws error
    - validateSchemaFile with binary/non-text file rejects
    - validateSchemaFile checks file extension is .ts, .js, or .json
    - validateSchemaFile checks file size is under a reasonable limit (e.g., 1MB)
  </behavior>
  <action>
1. Create src/security/schema-loader.ts:
   - Export class SchemaLoadError extending Error with: code (string), filePath (string)
   - Export async function validateSchemaFile(filePath: string): Promise<{ valid: boolean; filePath: string; size: number; extension: string }>
     - Check file exists (throw SchemaLoadError with "FILE_NOT_FOUND" if not)
     - Check extension is one of: .ts, .js, .json (throw "INVALID_EXTENSION" if not)
     - Check file size under 1MB limit (throw "FILE_TOO_LARGE" if not)
     - Read first 1024 bytes and check it looks like text (no null bytes in content -- throw "BINARY_FILE" if detected)
     - Return validation result with file metadata
   - This is a pre-flight check before dynamic import. The actual schema loading/import happens in Phase 2 -- this just validates the file is safe to attempt loading.

2. Create tests/security/schema-loading.test.ts:
   - Valid .ts file passes validation
   - Valid .json file passes validation
   - Non-existent file throws FILE_NOT_FOUND
   - File with .exe extension throws INVALID_EXTENSION
   - File over 1MB throws FILE_TOO_LARGE (create temp large file)
   - File with binary content throws BINARY_FILE
   - Returns correct metadata (size, extension, filePath)
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/mcps/schema-validator && bun test tests/security/schema-loading.test.ts</automated>
  </verify>
  <done>Schema file validator checks extension, size, and content before allowing load; rejects unsafe files with descriptive errors; all tests pass</done>
</task>

</tasks>

<verification>
- `cd schema-validator && bun test tests/security/` -- all security tests pass
- `cd schema-validator && bun test` -- full suite passes including server and format tests
- Path traversal with ../, encoded chars, null bytes all rejected
- Atomic write produces complete files (no partial content)
</verification>

<success_criteria>
- Path validator rejects traversal attacks, null bytes, and out-of-bounds paths
- Path validation errors include the rejected path in the message (per user decision)
- Atomic writes use write-file-atomic (temp + rename pattern)
- Schema file validator checks extension, size, and content before load
- Default allowed directory is process.cwd()
- All security tests pass with edge cases covered
</success_criteria>

<output>
After completion, create `.planning/phases/01-format-engine-server/01-03-SUMMARY.md`
</output>
