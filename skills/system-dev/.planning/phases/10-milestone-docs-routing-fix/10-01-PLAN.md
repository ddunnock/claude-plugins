---
phase: 10-milestone-docs-routing-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - SKILL.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - XCUT-01
  - XCUT-02
  - XCUT-03
  - XCUT-04

must_haves:
  truths:
    - "/system-dev:diagram appears in SKILL.md commands table and is routable by LLM agent"
    - "diagram slot type appears in SKILL.md slot types table with diag- prefix"
    - "diagrams/ directory appears in SKILL.md registry structure tree"
    - "All completed plan checkboxes in ROADMAP.md show [x]"
  artifacts:
    - path: "SKILL.md"
      provides: "Diagram command routing, slot type docs, registry structure"
      contains: "/system-dev:diagram"
    - path: ".planning/ROADMAP.md"
      provides: "Accurate completion state for all phases"
      contains: "[x] 08-02"
  key_links:
    - from: "SKILL.md commands table"
      to: "commands/diagram.md"
      via: "markdown link in table row"
      pattern: "\\| `/system-dev:diagram`.*commands/diagram\\.md"
    - from: "SKILL.md slot types table"
      to: "registry.py SLOT_TYPE_DIRS"
      via: "matching type name and prefix"
      pattern: "\\| diagram.*diag-"
---

<objective>
Close v1.1 audit gaps so `/system-dev:diagram` is discoverable in SKILL.md and all documentation reflects completed state.

Purpose: The diagram command and slot type are fully implemented but missing from SKILL.md, making them invisible to the LLM agent. Stale ROADMAP.md checkboxes misrepresent completion status.
Output: Updated SKILL.md with diagram command/slot type/registry entries; updated ROADMAP.md with accurate checkboxes.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-milestone-docs-routing-fix/10-RESEARCH.md
@SKILL.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add diagram command, slot type, and registry entry to SKILL.md</name>
  <files>SKILL.md</files>
  <action>
Three edits to SKILL.md, each matching the exact format of existing entries:

1. **Commands table** (after the `/system-dev:view` row at line 55): Add a new row:
   ```
   | `/system-dev:diagram` | Generate D2/Mermaid diagrams | [commands/diagram.md](commands/diagram.md) |
   ```

2. **Slot Types table** (after the `requirement-ref` row at line 64): Add a new row:
   ```
   | diagram | `diag-` | Generated D2/Mermaid diagram source from view data |
   ```
   Also update the table header from "Slot Types (Phase 1)" to "Slot Types" since diagram is a v1.1 addition.

3. **Registry structure tree** (after `requirement-refs/` at line 76): Add:
   ```
       diagrams/         # One JSON file per diagram slot
   ```

Also add a `view-specs/` entry after `config.json` if not already present:
   ```
     view-specs/         # Declarative view specification configs
   ```

Do NOT modify any Python code. Do NOT remove any imports from diagram_generator.py (research confirmed copy and defaultdict are actively used).
  </action>
  <verify>
    <automated>grep -c "system-dev:diagram" SKILL.md && grep -c "diag-" SKILL.md && grep -c "diagrams/" SKILL.md</automated>
  </verify>
  <done>SKILL.md commands table includes /system-dev:diagram with link to commands/diagram.md. Slot types table includes diagram with diag- prefix. Registry structure tree includes diagrams/ directory.</done>
</task>

<task type="auto">
  <name>Task 2: Fix stale ROADMAP.md checkboxes for completed plans</name>
  <files>.planning/ROADMAP.md</files>
  <action>
Mark three completed plans as done in ROADMAP.md:

1. Phase 8 line 103: Change `- [ ] 08-02-PLAN.md` to `- [x] 08-02-PLAN.md`
2. Phase 9 line 118: Change `- [ ] 09-01-PLAN.md` to `- [x] 09-01-PLAN.md`
3. Phase 9 line 119: Change `- [ ] 09-02-PLAN.md` to `- [x] 09-02-PLAN.md`

Also update the v1.1 milestone status marker from the current state to reflect that Phase 10 is now planned:
- Change Phase 10 line from `- [ ] **Phase 10:` to keep it unchecked (it is not yet executed)
- Ensure Phase 10 Plans line shows `**Plans:** 1 plan`

Do NOT change any other content in ROADMAP.md.
  </action>
  <verify>
    <automated>grep -c "\[x\] 08-02-PLAN" .planning/ROADMAP.md && grep -c "\[x\] 09-01-PLAN" .planning/ROADMAP.md && grep -c "\[x\] 09-02-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <done>All completed plans in ROADMAP.md show [x]. No stale unchecked boxes for plans that have been executed.</done>
</task>

</tasks>

<verification>
1. `grep "/system-dev:diagram" SKILL.md` returns the commands table row with link
2. `grep "diagram.*diag-" SKILL.md` returns the slot types table row
3. `grep "diagrams/" SKILL.md` returns the registry structure entry
4. `grep "\[ \]" .planning/ROADMAP.md` returns only Phase 10 plan (10-01) and Phase 10 phase checkbox -- no stale unchecked items for completed work
</verification>

<success_criteria>
- /system-dev:diagram is listed in SKILL.md commands table with correct link to commands/diagram.md
- diagram slot type is listed in SKILL.md slot types table with diag- prefix
- diagrams/ appears in SKILL.md registry structure tree
- All completed plans (08-02, 09-01, 09-02) show [x] in ROADMAP.md
- No Python code was modified
</success_criteria>

<output>
After completion, create `.planning/phases/10-milestone-docs-routing-fix/10-01-SUMMARY.md`
</output>
