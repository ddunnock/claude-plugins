---
phase: 08-diagram-generation-core
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - scripts/diagram_generator.py
  - scripts/view_assembler.py
  - schemas/view-spec.json
  - commands/diagram.md
  - tests/test_diagram_generator.py
autonomous: true
requirements: [DIAG-03, DIAG-09]

must_haves:
  truths:
    - "Diagram generator reads view handoff format and writes diagram slots through SlotAPI"
    - "Content-hash slot IDs enable update-on-change-only semantics"
    - "Generating diagrams only writes diagram-type slots, never modifies other slot types"
    - "/system-dev:diagram command orchestrates view assembly and diagram generation"
    - "Built-in view specs declare diagram_hint field for automatic format selection"
  artifacts:
    - path: "commands/diagram.md"
      provides: "Diagram command specification"
      min_lines: 30
    - path: "scripts/diagram_generator.py"
      provides: "Orchestration layer with SlotAPI writes"
      contains: "generate_diagram"
    - path: "schemas/view-spec.json"
      provides: "Optional diagram_hint field"
      contains: "diagram_hint"
  key_links:
    - from: "scripts/diagram_generator.py"
      to: "scripts/registry.py"
      via: "SlotAPI.ingest() for diagram slot writes"
      pattern: "api\\.ingest"
    - from: "scripts/diagram_generator.py"
      to: "scripts/view_assembler.py"
      via: "assemble_view() call for input data"
      pattern: "assemble_view"
    - from: "commands/diagram.md"
      to: "scripts/diagram_generator.py"
      via: "generate_diagram() orchestration call"
      pattern: "generate_diagram"
---

<objective>
Add orchestration layer for SlotAPI writes, diagram_hint on view specs, and the /system-dev:diagram command.

Purpose: Wire the pure generation functions from Plan 01 into the full pipeline: view assembly -> format selection -> generation -> SlotAPI write -> stdout output. This completes the end-to-end diagram generation flow.

Output: Updated `diagram_generator.py` with `generate_diagram()` orchestrator, `commands/diagram.md`, updated `view-spec.json` and `BUILTIN_SPECS` with `diagram_hint`, and integration tests.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-diagram-generation-core/08-CONTEXT.md
@.planning/phases/08-diagram-generation-core/08-RESEARCH.md
@.planning/phases/08-diagram-generation-core/08-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 outputs (scripts/diagram_generator.py) -->
```python
def generate_d2(view: dict) -> str: ...
def generate_mermaid(view: dict) -> str: ...
def _compute_diagram_slot_id(spec_name: str, source: str) -> str: ...
def _sanitize_id(slot_id: str) -> str: ...
```

<!-- From scripts/registry.py SlotAPI -->
```python
class SlotAPI:
    def ingest(self, slot_id: str, slot_type: str, content: dict, agent_id: str = "ingestion-engine") -> dict:
        """Accepts pre-determined slot_id. Returns {"status": "created"|"updated"|"conflict", "slot_id", "version"}"""
    def query(self, slot_type: str, filters: dict | None = None) -> list[dict]: ...
```

<!-- From scripts/view_assembler.py -->
```python
def assemble_view(api: SlotAPI, spec: dict, workspace_root: str, schemas_dir: str) -> dict: ...
def get_builtin_spec(name: str, parameters: dict | None = None) -> dict: ...
def load_view_spec(spec_path: str, parameters: dict | None = None, schemas_dir: str | None = None) -> dict: ...
BUILTIN_SPECS: dict[str, dict]  # Add diagram_hint to each entry
```

<!-- From schemas/view-spec.json (current required: name, description, scope_patterns) -->
<!-- diagram_hint is OPTIONAL, not added to required array -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Orchestration layer, diagram_hint, and view-spec schema update</name>
  <files>scripts/diagram_generator.py, scripts/view_assembler.py, schemas/view-spec.json, tests/test_diagram_generator.py</files>
  <behavior>
    - Test: generate_diagram() calls assemble_view, selects format from diagram_hint, generates source, writes via SlotAPI.ingest()
    - Test: generate_diagram() with --format override uses override instead of diagram_hint
    - Test: generate_diagram() errors when no diagram_hint and no --format specified
    - Test: generate_diagram() returns result dict with status, slot_id, format, source, and stdout header
    - Test: Content-hash slot_id is deterministic (same source -> same ID)
    - Test: Update-on-change: if ingest() returns "conflict" (existing slot version > 1), result status reflects it
    - Test: Only "diagram" type slots are written (mock SlotAPI, verify no calls to create/update with other types)
    - Test: BUILTIN_SPECS entries have diagram_hint values matching CONTEXT.md decisions
    - Test: view-spec.json schema accepts specs with diagram_hint field
    - Test: view-spec.json schema accepts specs without diagram_hint field (backward compatible)
  </behavior>
  <action>
    1. Add `generate_diagram()` orchestration function to `scripts/diagram_generator.py`:
       ```python
       def generate_diagram(
           api: SlotAPI,
           spec: dict,
           workspace_root: str,
           schemas_dir: str,
           format_override: str | None = None,
       ) -> dict:
       ```
       - Call `assemble_view(api, spec, workspace_root, schemas_dir)` to get view data
       - Determine format: use `format_override` if provided, else `spec.get("diagram_hint")`. If neither, raise ValueError("No diagram format specified. Use --format d2 or --format mermaid, or add diagram_hint to view spec.")
       - Map: "structural" -> generate_d2(), "behavioral" -> generate_mermaid(). Also accept literal "d2"/"mermaid" from --format override.
       - Compute slot_id via `_compute_diagram_slot_id(spec["name"], source)`
       - Build diagram slot content dict with all required fields from diagram.json schema (slot_id, slot_type="diagram", name=f"diagram-{spec['name']}", version=1, format, diagram_type, source, source_view_spec=spec["name"], source_snapshot_id=view["snapshot_id"], slot_count=view["total_slots"], gap_count=view["total_gaps"], created_at, updated_at)
       - Validate against schemas/diagram.json using SchemaValidator
       - Write via `api.ingest(slot_id, "diagram", content, agent_id="diagram-generator")` (DIAG-09: only writes "diagram" type)
       - Return dict: {"status": ingest_result["status"], "slot_id": slot_id, "format": format_str, "diagram_type": diagram_type, "source": source, "slot_count": ..., "gap_count": ...}

    2. Add `diagram_hint` to BUILTIN_SPECS in `scripts/view_assembler.py` per user decisions:
       - "system-overview": `"diagram_hint": "structural"`
       - "traceability-chain": `"diagram_hint": "behavioral"`
       - "component-detail": `"diagram_hint": "structural"`
       - "interface-map": `"diagram_hint": "structural"`
       - "gap-report": no diagram_hint (omit the key entirely)

    3. Add `diagram_hint` to `schemas/view-spec.json` properties (NOT to required array):
       ```json
       "diagram_hint": {
         "type": "string",
         "enum": ["structural", "behavioral"],
         "description": "Hint for diagram format: structural -> D2, behavioral -> Mermaid"
       }
       ```

    4. Add integration tests to `tests/test_diagram_generator.py`:
       - Mock SlotAPI with a spy that records ingest() calls
       - Mock assemble_view to return the _make_view() fixture
       - Test the full generate_diagram() flow
       - Verify only "diagram" type slots written (DIAG-09)
       - Test format override logic
       - Test error on missing format
       - Test BUILTIN_SPECS diagram_hint values
       - Test view-spec.json schema with and without diagram_hint
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/skills/system-dev && python -m pytest tests/test_diagram_generator.py -x -q</automated>
  </verify>
  <done>generate_diagram() orchestrates view assembly -> generation -> SlotAPI write. diagram_hint on built-in specs per CONTEXT.md decisions. view-spec.json accepts optional diagram_hint. Only diagram-type slots written (DIAG-09). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create /system-dev:diagram command specification</name>
  <files>commands/diagram.md</files>
  <action>
    Create `commands/diagram.md` following the pattern established by `commands/view.md`:

    ```markdown
    # /system-dev:diagram

    Generate D2 structural or Mermaid behavioral diagrams from view specifications.

    ## Invocation

    /system-dev:diagram <spec_name_or_path> [--format d2|mermaid] [--param key=value]

    **Parameters:**
    - `spec_name_or_path` (required): Name of a built-in spec (system-overview, traceability-chain, component-detail, interface-map) OR a file path to a view-spec JSON file. No ad-hoc patterns -- diagrams are intentional artifacts.
    - `--format` (optional): Override diagram format. `d2` for D2 structural, `mermaid` for Mermaid behavioral. Defaults to the spec's `diagram_hint` field. Error if neither specified.
    - `--param` (optional, repeatable): Parameter for parameterized specs (e.g., `--param component_id="Auth Service"`)

    ## Workflow

    1. **Determine spec**: Same as /system-dev:view (built-in via get_builtin_spec, file path via load_view_spec). Reject ad-hoc patterns with error message.
    2. **Resolve parameters**: Apply --param values via get_builtin_spec(name, parameters) or load_view_spec(path, parameters, schemas_dir).
    3. **Initialize**: Load SlotAPI from .system-dev/ workspace and locate schemas/ directory.
    4. **Generate**: Call generate_diagram(api, spec, workspace_root, schemas_dir, format_override) from scripts/diagram_generator.py.
    5. **Display**: Print header (format, diagram type, slot_id, storage path) then the diagram source to stdout.

    ## Built-in Diagram Specs

    | Name | Default Format | Diagram Type |
    |------|---------------|--------------|
    | system-overview | D2 | structural |
    | traceability-chain | Mermaid | behavioral |
    | component-detail | D2 | structural |
    | interface-map | D2 | structural |
    | gap-report | (none -- requires --format) | varies |

    ## Output Format

    Diagram source is printed to stdout with a header block:
    --- Diagram: {spec_name} ---
    Format: {d2|mermaid}
    Type: {structural|behavioral}
    Slot: {slot_id}
    Status: {created|updated|unchanged}
    Slots: {N}, Gaps: {N}
    ---
    {diagram source code}

    ## Error Handling

    - No spec found: List available built-in specs
    - No format (no diagram_hint, no --format): Error with guidance
    - Empty view (no slots matched): Generate minimal diagram with comment header only, warn user
    - Invalid spec schema: Error from SchemaValidator with field-level details
    ```

    Ensure the command references `scripts/diagram_generator.py:generate_diagram()` as the entry point. Do NOT include ad-hoc pattern support (per user decision: diagrams are intentional artifacts from named specs only).
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/skills/system-dev && test -f commands/diagram.md && grep -q "generate_diagram" commands/diagram.md && grep -q "system-dev:diagram" commands/diagram.md && echo "PASS"</automated>
  </verify>
  <done>commands/diagram.md exists with complete command specification. References generate_diagram() entry point. Supports --format override and --param. Rejects ad-hoc patterns. Documents all built-in specs with their default formats.</done>
</task>

</tasks>

<verification>
- All tests in test_diagram_generator.py pass
- Full test suite passes: `python -m pytest tests/ -x -q`
- generate_diagram() writes only "diagram" type slots via SlotAPI.ingest()
- BUILTIN_SPECS have correct diagram_hint values
- view-spec.json accepts optional diagram_hint without breaking existing specs
- commands/diagram.md follows established command pattern
</verification>

<success_criteria>
- generate_diagram() reads view handoff and writes diagram slots via SlotAPI (DIAG-03)
- Only diagram-type slots are written during generation (DIAG-09)
- /system-dev:diagram command is fully specified with format override support
- Built-in specs have diagram_hint per user decisions
- All new + existing tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/08-diagram-generation-core/08-02-SUMMARY.md`
</output>
