---
phase: 07-view-quality-handoff
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - schemas/view.json
  - schemas/view-spec.json
  - scripts/view_assembler.py
  - tests/test_view_assembler.py
autonomous: true
requirements: [VIEW-03, VIEW-09, VIEW-12]

must_haves:
  truths:
    - "Slots in assembled views are sorted by relationship density (most-connected first)"
    - "Given identical registry state and view-spec, assembler produces byte-identical output (excluding assembled_at and timing)"
    - "Content-hash snapshot_id is deterministic for same registry content"
    - "View-specs can override ranking method via optional ranking field"
    - "Assembly completes within 500ms for 100-slot registries"
  artifacts:
    - path: "scripts/view_assembler.py"
      provides: "_compute_density_scores(), _rank_slots(), content-hash snapshot_id"
      contains: "_compute_density_scores"
    - path: "schemas/view.json"
      provides: "Extended schema with edges, metadata, slot relationships fields"
      contains: "edges"
    - path: "schemas/view-spec.json"
      provides: "Optional ranking field"
      contains: "ranking"
    - path: "tests/test_view_assembler.py"
      provides: "Tests for ranking, determinism, performance"
      contains: "test_density"
  key_links:
    - from: "scripts/view_assembler.py"
      to: "schemas/view.json"
      via: "SchemaValidator validates assembled output"
      pattern: "validate_or_raise.*view"
    - from: "scripts/view_assembler.py:_compute_density_scores"
      to: "scripts/view_assembler.py:assemble_view"
      via: "scores computed from snapshot, applied to sort sections"
      pattern: "_compute_density_scores"
    - from: "scripts/view_assembler.py:capture_snapshot"
      to: "hashlib.sha256"
      via: "content-hash replaces uuid4 for determinism"
      pattern: "hashlib.sha256"
---

<objective>
Add relevance ranking, deterministic output, and performance instrumentation to the view assembler.

Purpose: Slots in contextual views must be ranked by relationship density so hub components surface first (VIEW-03). Output must be deterministic for same input (VIEW-09). Assembly must meet performance targets (VIEW-12). These are internal computation changes that Plan 02 builds on for handoff format and logging.

Output: Updated view_assembler.py with ranking and determinism, extended schemas for Plan 02 compatibility, new tests.
</objective>

<execution_context>
@/Users/dunnock/.claude/get-shit-done/workflows/execute-plan.md
@/Users/dunnock/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-view-quality-handoff/07-CONTEXT.md
@.planning/phases/07-view-quality-handoff/07-RESEARCH.md

@scripts/view_assembler.py
@schemas/view.json
@schemas/view-spec.json
@tests/test_view_assembler.py

<interfaces>
<!-- Key types and contracts the executor needs. -->

From scripts/view_assembler.py:
```python
def capture_snapshot(api: SlotAPI) -> dict:
    # Returns: {snapshot_id: str(uuid4), captured_at: str, slots_by_type: dict}

def assemble_view(api: SlotAPI, spec: dict, workspace_root: str, schemas_dir: str) -> dict:
    # Returns assembled view dict conforming to view.json schema

def _organize_hierarchically(sections: list[dict]) -> list[dict]:
    # Builds comp_interface_map, intf_contract_map, linked_interfaces, linked_contracts
    # These same relationship structures are reusable for density scoring

BUILTIN_SPECS: dict[str, dict]  # Built-in view specifications
```

From schemas/view.json (current top-level):
```json
"required": ["spec_name", "format_version", "assembled_at", "snapshot_id", "total_slots", "total_gaps", "gap_summary", "sections", "gaps"],
"additionalProperties": false
```

From schemas/view-spec.json (current top-level):
```json
"required": ["name", "description", "scope_patterns"],
"additionalProperties": false
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend schemas and add ranking + determinism to view assembler</name>
  <files>schemas/view.json, schemas/view-spec.json, scripts/view_assembler.py</files>
  <action>
**CRITICAL ORDER: Update schemas BEFORE code changes.** The existing `additionalProperties: false` on both schemas will reject new fields if code emits them before schema allows them.

**Step 1 — Update schemas/view-spec.json:**
Add optional `ranking` field to the top-level properties (alongside name, description, scope_patterns, parameters):
```json
"ranking": {
  "type": "string",
  "enum": ["density", "alphabetical", "none"],
  "description": "Ranking method for slots. Default is density if omitted."
}
```
Do NOT add ranking to the `required` array — it is optional.

**Step 2 — Update schemas/view.json:**
Add these new fields to the top-level properties (required for Plan 02 compatibility — add them now so schema is ready, but they will be populated as empty defaults until Plan 02 implements them):

1. `edges` array (add to both properties AND required):
```json
"edges": {
  "type": "array",
  "items": {
    "type": "object",
    "required": ["source_id", "target_id", "relationship_type"],
    "properties": {
      "source_id": {"type": "string", "minLength": 1},
      "target_id": {"type": "string", "minLength": 1},
      "relationship_type": {"type": "string", "minLength": 1}
    },
    "additionalProperties": false
  }
}
```

2. `metadata` object (add to both properties AND required):
```json
"metadata": {
  "type": "object",
  "required": ["elapsed_ms", "ranking_method", "section_counts"],
  "properties": {
    "elapsed_ms": {"type": "number", "minimum": 0},
    "ranking_method": {"type": "string"},
    "section_counts": {
      "type": "object",
      "additionalProperties": {"type": "integer", "minimum": 0}
    }
  },
  "additionalProperties": true
}
```

Bump `format_version` default expectation to "1.1" (the pattern `^\d+\.\d+$` already accepts this).

**Step 3 — Add `_compute_density_scores()` to view_assembler.py:**
Implement per the research code example. Counts direct + one-hop connections from interfaces, contracts, and traceability-links across the FULL snapshot (not just matched slots — Pitfall 4). Use a separate `{slot_id: score}` dict, never mutate snapshot data (anti-pattern from research).

**Step 4 — Add `_rank_slots()` to view_assembler.py:**
```python
def _rank_slots(slots: list[dict], scores: dict[str, int], method: str = "density") -> list[dict]:
```
- `"density"`: sort by (-score, -version, name) using stable sort
- `"alphabetical"`: sort by (name, slot_id)
- `"none"`: return slots unchanged (preserve snapshot order)
Returns a NEW list (do not mutate input).

**Step 5 — Replace uuid4() with content-hash in `capture_snapshot()`:**
```python
import hashlib
serialized = json.dumps(slots_by_type, sort_keys=True, separators=(',', ':'))
snapshot_id = hashlib.sha256(serialized.encode()).hexdigest()[:16]
```
Remove the `from uuid import uuid4` import. Add `import hashlib` and `import time`.

**Step 6 — Update `assemble_view()` to integrate ranking, timing, and new output fields:**
- Add `time.perf_counter()` at start and end for elapsed_ms
- After `_organize_hierarchically()`, compute density scores from snapshot via `_compute_density_scores(snapshot)`
- Determine ranking method: `spec.get("ranking", "density")`
- Apply `_rank_slots()` to each section's slots list
- Build `section_counts` dict: `{section["slot_type"]: len(section["slots"]) for section in sections}`
- Add to assembled output:
  - `"edges": []` (empty placeholder — Plan 02 populates)
  - `"metadata": {"elapsed_ms": round(elapsed_ms, 2), "ranking_method": ranking_method, "section_counts": section_counts}`
- Change `"format_version"` from `"1.0"` to `"1.1"`

**DO NOT** add structured logging calls yet — that is Plan 02's responsibility.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/skills/system-dev && python3 -m pytest tests/test_view_assembler.py -x -q</automated>
  </verify>
  <done>
- _compute_density_scores() exists and computes relationship density from full snapshot
- _rank_slots() sorts by density desc, version desc, name asc (or alphabetical/none)
- capture_snapshot() uses content-hash (SHA-256 truncated to 16 hex chars) instead of uuid4
- assemble_view() outputs edges (empty array), metadata (with elapsed_ms, ranking_method, section_counts), format_version "1.1"
- view-spec.json accepts optional ranking field
- view.json accepts edges and metadata fields
- All 73 existing tests pass (some may need minor updates for new output fields)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add tests for ranking, determinism, and performance</name>
  <files>tests/test_view_assembler.py</files>
  <behavior>
    - test_density_scores_count_interface_connections: Component with 2 interfaces scores higher than component with 0
    - test_density_scores_count_one_hop_contracts: Component with interface that has contracts gets one-hop credit
    - test_density_scores_count_traceability_links: Slots referenced by traceability-links get density credit
    - test_density_scores_computed_from_full_snapshot: Same component gets same score regardless of which view includes it
    - test_rank_slots_density_ordering: Higher density slots appear first in section
    - test_rank_slots_tiebreak_version_then_name: Same density ties broken by version desc then name asc
    - test_rank_slots_alphabetical_method: ranking="alphabetical" sorts by name
    - test_rank_slots_none_method: ranking="none" preserves original order
    - test_ranking_override_in_spec: View-spec with "ranking": "alphabetical" uses alphabetical instead of density
    - test_deterministic_snapshot_id: Same registry content produces same snapshot_id across calls
    - test_deterministic_snapshot_id_different_content: Different content produces different snapshot_id
    - test_deterministic_output: Two assemble_view calls with same input produce identical output (excluding assembled_at and timing)
    - test_format_version_is_1_1: Assembled output has format_version "1.1"
    - test_assembled_output_has_edges_array: Output contains edges key (empty array for now)
    - test_assembled_output_has_metadata: Output contains metadata with elapsed_ms, ranking_method, section_counts
    - test_performance_100_slots: Assembly of 100-slot registry completes in under 500ms
  </behavior>
  <action>
Add a new test class `TestRanking` with tests for density scoring, slot ranking, and ranking method override.

Add a new test class `TestDeterminism` with tests for content-hash snapshot_id and deterministic full output.

Add a new test class `TestPerformance` with a test that creates 100 slots across component/interface/contract types and asserts assembly time < 500ms.

Import `_compute_density_scores` and `_rank_slots` from `scripts.view_assembler`.

For determinism tests, create a helper `_deterministic_content(view)` that returns the view dict with `assembled_at` and `metadata.elapsed_ms` stripped, for comparison purposes.

For the performance test, use `time.perf_counter()` to measure wall-clock time. Create slots programmatically in a loop using `api.create()`.

For ranking override test, create a spec with `"ranking": "alphabetical"` and verify slots are sorted by name rather than density.
  </action>
  <verify>
    <automated>cd /Users/dunnock/projects/claude-plugins/skills/system-dev && python3 -m pytest tests/test_view_assembler.py -x -q -k "rank or deterministic or determinism or perf or format_version or edges or metadata"</automated>
  </verify>
  <done>
- At least 16 new tests covering ranking, determinism, and performance
- All new tests pass
- All existing tests still pass
- Full test suite green: python3 -m pytest tests/ -q
  </done>
</task>

</tasks>

<verification>
```bash
# All view assembler tests pass
cd /Users/dunnock/projects/claude-plugins/skills/system-dev && python3 -m pytest tests/test_view_assembler.py -x -q

# Full test suite passes
python3 -m pytest tests/ -q

# Schema validates correctly
python3 -c "
import json
from jsonschema import Draft202012Validator
with open('schemas/view.json') as f:
    schema = json.load(f)
Draft202012Validator.check_schema(schema)
print('view.json schema valid')
with open('schemas/view-spec.json') as f:
    schema = json.load(f)
Draft202012Validator.check_schema(schema)
print('view-spec.json schema valid')
"
```
</verification>

<success_criteria>
- Density ranking sorts hub components (many connections) to top of views
- Content-hash snapshot_id is deterministic for same registry state
- Sorted output within sections is deterministic (density desc, version desc, name asc)
- View-spec ranking field overrides default density ranking
- format_version is "1.1" in assembled output
- Assembly of 100-slot registry completes in < 500ms
- All existing + new tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/07-view-quality-handoff/07-01-SUMMARY.md`
</output>
